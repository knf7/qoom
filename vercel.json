{
  "buildCommand": "npm run build -w @qoom/types && npm run build -w @qoom/ui && npm run build -w @qoom/web",
  "outputDirectory": "dist",
  "installCommand": "npm ci",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
